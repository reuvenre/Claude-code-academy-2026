# העתק ל-.env.local ומלא ערכים. .env.local נמצא ב-gitignore.
# כתובת האתר הקנונית (לשימוש ב-sitemap, canonical, JSON-LD, llms.txt)
# שים לב: המשתנה נקרא בזמן build (לא בזמן start). בפריסה מגדירים אותו בסביבת ה-build.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
