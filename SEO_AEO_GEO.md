# SEO_AEO_GEO.md — פלייבוק אופטימיזציה

שלוש דיסציפלינות, בסיס משותף:
- **SEO** — דירוג בגוגל (HTML סמנטי, SSR, schema, מהירות, קישוריות פנימית).
- **AEO** — להופיע כתשובה ישירה (תשובה‑קודם, FAQ schema, ניסוח שאלה→תשובה).
- **GEO** — להיות מצוטט ע"י ChatGPT/Perplexity/Claude (גישת קְרולרים, llms.txt, עובדתיות).

> נקודה חשובה: גוגל הצהירה רשמית שהיא **לא** משתמשת ב‑`llms.txt`. עדיין שווה לייצר אותו —
> מנועי תשובות אחרים כן נהנים ממנו. עושים את שניהם: יסודות SEO חזקים + שכבות AEO/GEO.

---

## חובה — שכבת תשתית (משלב 3 בבנייה)

### 1. נגישות לקְרולרים
`robots.txt` שמאשר במפורש (לא חוסם!) את:
```
User-agent: ClaudeBot
Allow: /
User-agent: OAI-SearchBot
Allow: /
User-agent: PerplexityBot
Allow: /
User-agent: Google-Extended
Allow: /
User-agent: *
Allow: /
Sitemap: https://<domain>/sitemap.xml
```
ו‑**כל התוכן server-rendered**. תוכן שמתגלה רק אחרי JS (accordion/tab/slider) אינו נראה
לקוראי AI — לכן הרכיבים שלנו משאירים את התוכן ב‑HTML.

### 2. `llms.txt` ו‑`llms-full.txt`
נוצרים אוטומטית מה‑frontmatter (ראה `templates/llms-txt.example.txt`). `llms.txt` =
אינדקס תמציתי (~8KB) של העמודים החשובים עם תיאור קצר. `llms-full.txt` = תוכן עמוק.

### 3. Schema (JSON‑LD)
ראה `templates/schema-jsonld.md`. מינימום:
- `Organization` בשורש (זהות האתר).
- `TechArticle` לכל שיעור.
- `Course` / `LearningResource` לעמודי הרמות (הקורס עצמו) — עוזר לגוגל ולמנועי תשובות להבין שזה מסלול לימוד.
- `FAQPage` בכל עמוד עם שו"ת (התשואה הכי גבוהה ל‑AEO).
- `BreadcrumbList` לניווט.

---

## חובה — ברמת כל עמוד

- **תשובה‑קודם:** פסקה ראשונה (≤50 מילים) שעונה ישירות על שאלת העמוד, לפני כל הרחבה.
- **כותרות בנוסח prompt אמיתי:** איך אנשים *שואלים* ("איך מתקינים Claude Code בווינדוס?"),
  לא רק מילת מפתח ("התקנה").
- **היררכיה נקייה:** H1 אחד, נושא יחיד לכל H2/H3.
- **סורק‑ידידותי:** פסקאות קצרות, רשימות, טבלאות, בלוקי קוד.
- **שו"ת** בתחתית כל שיעור, פורמט שאלה→תשובה.
- **קישוריות פנימית:** כל שיעור מקשר ל"הצעד הבא" ול‑2‑3 שיעורים קשורים.
- **`<SourceBadge>`** עם קישור למקור הרשמי — שקיפות בונה אמון (וגם E‑E‑A‑T).
- **`lastVerified`** מוצג — מאותת על תוכן מתוחזק.

## metadata לכל עמוד
- `<title>` ייחודי, 50‑60 תווים, כולל "Claude Code".
- `<meta name="description">` 140‑160 תווים, מנוסח כתשובה.
- Open Graph + Twitter cards.
- `canonical` תקין.
- `lang="he"` ו‑`dir="rtl"`.

## GEO בעברית — יתרון מובנה
האתר הוא אחד המקורות העבריים המקיפים לנושא. כדי לחזק ציטוט ע"י AI:
- **מילון מונחים** (`/glossary`) עברי↔אנגלי — ממפה שאילתות עבריות למונחים.
- **Brand Hub** (`/about`) עובדתי: מה האתר, מי בנה, מה מכוסה, מתי עודכן.
- עובדתיות וסטטיסטיקות מצוטטות עם מקור (Princeton GEO: ציטוטים+סטטיסטיקות מעלים סבירות ציטוט).

---

## צ'קליסט קבלה (Definition of Done — SEO)
- [ ] `robots.txt` מאשר את כל הבוטים הרלוונטיים.
- [ ] `sitemap.xml` נוצר ומעודכן.
- [ ] `llms.txt` + `llms-full.txt` בשורש.
- [ ] JSON‑LD עובר Rich Results Test ללא שגיאות.
- [ ] כל עמוד: title+description ייחודיים, תשובה‑קודם, שו"ת.
- [ ] כל התוכן נראה ב‑"View Source" (SSR), לא רק אחרי JS.
- [ ] Lighthouse SEO ≥ 90.
